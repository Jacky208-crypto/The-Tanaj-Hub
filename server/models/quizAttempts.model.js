// models/quizAttempts.model.js
//
// Data-access layer for the quiz_attempts table (id, user_id, quiz_label,
// score, total, created_at).
//
// Would hold:
//   - findByUser(userId)
//   - create(userId, { quizLabel, score, total })
